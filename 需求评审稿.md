# 需求评审稿

> 本文是 Cursor Skill **float-kit** 的设计验收稿：Skill 怎么分层、Agent 怎么用；基础场景用对应 demo 验收写程序产出。

---

## 一、需求内容

**一句话**：把画中画 / 闪控窗 / 闪控球的分流、资料、误用清单、骨架收进一个自包含 Skill，供 Agent 按设计产出。

**背景 / 痛点**：三类窗易用错；`create` 被当成已显示；权限和互斥常漏。资料要落在 Skill 内，不依赖其他 Skill、不日常抓官网。

**范围**
- 做：Skill 目录设计（入口、分册、落盘、骨架、评审模板）；三种基础窗 + 可选绑定/防窥。
- 不做：子窗、`TYPE_FLOAT`、模态窗；改 HarmonyOS 对外接口；依赖其他 Skill。

**成功标准**：目录与 `SKILL.md` 工作流符合第二章；写程序模式对三个基础场景能给出与第六章 demo 同调用顺序的代码。

---

## 二、规格设计

Skill **如何设计**（可判定）：

| # | 规则 | 说明 |
|---|------|------|
| 1 | 入口只有 `SKILL.md`：分流表、铁律、五种模式、按模式读什么 | 结构 |
| 2 | `docs/` 是接口/权限/错误码落盘；日常以它为契约，不 WebFetch | 资料 |
| 3 | `reference-*.md` 只放误用/权限/绑定要点，不列接口表 | 结构 |
| 4 | `examples.md` 是写程序骨架；`req-template.md` 是评审六章，不引用其他 Skill | 结构 |
| 5 | 先分流再动手；范围外停手；`create≠start`、主窗前台、先探测；未绑定三类窗互斥 | 行为 |
| 6 | 写程序默认 Stage ArkTS；PiP API12+ 用 typeNode demo；闪控窗要 `FLOAT_VIEW` 弹窗；闪控球 `USE_FLOAT_BALL` 不弹窗 | 写程序 |

**关键约束**：名 `float-kit`；目录 `.cursor/skills/float-kit/`；仅 Stage。

---

## 三、功能点

1. **F1** 分流：pip / float_view / floating_ball / bind / 防窥 / 范围外
2. **F2** 接口问答：按 `docs/` 章节答，不通读全文
3. **F3** 查错：E 格式标位置
4. **F4** 写程序：对齐第六章三个基础场景 demo
5. **F5** 需求设计：只读本目录 `req-template.md`

---

## 四、方案设计

**思路**：入口薄、资料落盘、误用清单、骨架、评审模板五块拆开。不改窗口框架，不设计新系统接口。

```text
float-kit/
├── SKILL.md                 # 分流 + 模式 + 铁律
├── reference-pip.md
├── reference-float-view.md
├── reference-floating-ball.md
├── examples.md              # 完整骨架
├── req-template.md
└── docs/                    # 接口落盘
```

**主流程**：命中 Skill → 分流 → 按模式读最少文件 → 产出。刷新官网仅用户明确要求。

**影响面**：本 Skill 目录与 GitHub 仓库。不改 WMS。

---

## 五、接口设计

不设计。不新增、不修改 HarmonyOS 对外接口。

---

## 六、测试用例

验收写程序：三个基础场景应对齐下列 demo（调用顺序不得改；完整版见 `examples.md`）。

### 场景 1 画中画

视频退出仍以系统小窗播放。主窗 show 后按钮触发；`create` 后必须 `startPiP`；以 `STARTED` 为准。

```ts
import { PiPWindow, typeNode } from '@kit.ArkUI';
import { common } from '@kit.AbilityKit';

@Entry
@Component
struct PipDemo {
  private xCtrl: XComponentController = new XComponentController();
  private pip?: PiPWindow.PiPController;

  private async start(): Promise<void> {
    if (!PiPWindow.isPiPEnabled()) {
      return;
    }
    const uiCtx = this.getUIContext();
    const ctx = uiCtx.getHostContext() as common.UIAbilityContext;
    const node = typeNode.createNode(uiCtx, 'XComponent', {
      type: XComponentType.SURFACE,
      controller: this.xCtrl
    });
    this.pip = await PiPWindow.create({
      context: ctx,
      componentController: this.xCtrl,
      templateType: PiPWindow.PiPTemplateType.VIDEO_PLAY,
      contentWidth: 1920,
      contentHeight: 1080
    }, node);
    this.pip.on('stateChange', (state: PiPWindow.PiPState) => {
      console.info(`pip ${state}`);
    });
    await this.pip.startPiP();
  }

  build() {
    Button('start PiP').onClick(() => { this.start(); })
  }
}
```

### 场景 2 闪控窗

退后台仍显示应用自己的页。`FLOAT_VIEW` 要弹窗；`start()` Promise ≠ 已显示。

```ts
import { floatView } from '@kit.ArkUI';
import { common } from '@kit.AbilityKit';

@Entry
@Component
struct FloatViewDemo {
  private fv?: floatView.FloatViewController;

  private async start(): Promise<void> {
    if (!floatView.isFloatViewEnabled()) {
      return;
    }
    const ctx = this.getUIContext().getHostContext() as common.UIAbilityContext;
    await requestPerms(ctx, ['ohos.permission.FLOAT_VIEW']);
    this.fv = await floatView.create({
      context: ctx,
      templateType: floatView.FloatViewTemplateType.ROUNDED_RECTANGLE
    });
    this.fv.onStateChange((info) => {
      console.info(`fv ${JSON.stringify(info)}`);
    });
    await this.fv.setUIContext('pages/FloatPanel');
    await this.fv.start();
  }

  build() {
    Button('start float view').onClick(() => { this.start(); })
  }
}
```

### 场景 3 闪控球

贴边小球，只展示标题/内容。`USE_FLOAT_BALL` 声明 + ACL，不弹窗。

```ts
import { floatingBall } from '@kit.ArkUI';
import { common } from '@kit.AbilityKit';

@Entry
@Component
struct BallDemo {
  private fb?: floatingBall.FloatingBallController;

  private async start(): Promise<void> {
    if (!floatingBall.isFloatingBallEnabled()) {
      return;
    }
    const ctx = this.getUIContext().getHostContext() as common.UIAbilityContext;
    this.fb = await floatingBall.create({ context: ctx });
    this.fb.on('click', () => { this.fb?.restoreMainWindow(); });
    await this.fb.startFloatingBall({
      template: floatingBall.FloatingBallTemplate.EMPHATIC,
      title: '盯盘',
      content: '沪指 +0.8%'
    });
  }

  build() {
    Button('start ball').onClick(() => { this.start(); })
  }
}
```

| # | 场景 | 期望 |
|---|------|------|
| 1 | 画中画 demo | 含探测、`create`+`startPiP`、状态回调；不在 `aboutToAppear` 里 start |
| 2 | 闪控窗 demo | 含 `FLOAT_VIEW` 弹窗、`setUIContext`、`onStateChange` |
| 3 | 闪控球 demo | 不弹窗；`startFloatingBall` 带 title；有 click |

**必过门禁**：单测 □ · 编译 □ · codecheck □ · 兼容结论 □（本次不适用；对照 demo 人工验收写程序产出）

---

## 评审结论（会后填）

- [ ] 通过
- [ ] 修改后通过（待办：……）
- [ ] 不通过（原因：……）

**待办 / 责任人**：……
