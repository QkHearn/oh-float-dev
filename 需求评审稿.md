# 需求评审稿

> 本文是 Cursor Skill **float-kit** 的设计验收稿：Skill 怎么分层、Agent 怎么用；基础场景用对应 demo 验收写程序产出。

---

## 一、需求内容

**一句话**：把画中画 / 闪控窗 / 闪控球的分流、资料、误用清单、骨架收进一个自包含 Skill，供 Agent 按设计产出。

**背景 / 痛点**：三类窗易用错；`create` 被当成已显示；权限和互斥常漏。资料要落在 Skill 内，不依赖其他 Skill、不日常抓官网。

**范围**
- 做：Skill 目录设计（入口、分册、落盘、骨架、评审模板）；三种基础窗 + 可选绑定/防窥；用户说某一种样式时映射到对应系统模板。
- 不做：子窗、`TYPE_FLOAT`、模态窗；改 HarmonyOS 对外接口；依赖其他 Skill。

**成功标准**：目录与 `SKILL.md` 工作流符合第二章；写程序按第六章 **画中画 4 + 闪控窗 2 + 闪控球 4** 各出对应模板，调用顺序与共用 demo 相同；有应用工程则改现有 `Index.ets` 等文件，不另存 demo 页。

---

## 二、规格设计

Skill **如何设计**（可判定）：

| # | 规则 | 说明 |
|---|------|------|
| 1 | 入口只有 `SKILL.md`：分流表、铁律、五种模式、按模式读什么 | 结构 |
| 2 | `docs/` 是接口/权限/错误码落盘；日常以它为契约，不 WebFetch | 资料 |
| 3 | `reference-*.md` 只放误用/权限/绑定要点，不列接口表 | 结构 |
| 4 | `examples.md` 是写程序骨架；`req-template.md` 是评审六章，不引用其他 Skill | 结构 |
| 5 | 先分流再动手；范围外停手；`create≠start`、主窗前台、先探测；未绑定三类窗互斥 | 行为 |
| 6 | 写程序默认 Stage ArkTS；PiP API12+ 用 typeNode demo；闪控窗要 `FLOAT_VIEW` 弹窗；闪控球 `USE_FLOAT_BALL` 不弹窗 | 写程序 |
| 7 | 用户说某一种样式 → 用 `SKILL.md` 模板对照选枚举；只改 template / 球必传字段 / PiP `controlGroups`（须同族），不改调用顺序。未说样式才用默认 | 写程序 |
| 8 | 写程序 / 查错能定位到应用文件就直接改；找不到 `**/src/main/ets/pages` 就停手问路径，不往框架仓写 ArkTS | 落盘 |

**关键约束**：名 `float-kit`；目录 `.cursor/skills/float-kit/`；仅 Stage。模板对照见第四章。

---

## 三、功能点

1. **F1** 分流：pip / float_view / floating_ball / bind / 防窥 / 范围外
2. **F2** 接口问答：按 `docs/` 章节答，不通读全文
3. **F3** 查错：E 格式标位置；能定位到工作区文件就改掉
4. **F4** 写程序：对齐第六章 4+2+4 用例；只改模板枚举和必传字段；并入现有 `Index.ets`
5. **F5** 需求设计：只读本目录 `req-template.md`
6. **F6** 样式映射：画中画 4 种、闪控球 4 种、闪控窗 2 种窗框
7. **F7** 工程/语法/视觉：写程序必读 `stage-layout` / `arkui-syntax` / `ui-style` / `examples`

---

## 四、方案设计

**思路**：入口薄、资料落盘、误用清单、骨架、评审模板五块拆开。不改窗口框架，不设计新系统接口。

```text
float-kit/
├── SKILL.md                 # 分流 + 模板对照 + 模式 + 铁律
├── reference-pip.md
├── reference-float-view.md
├── reference-floating-ball.md
├── examples.md              # 骨架；先合并进 Index，完整页只作对照
├── req-template.md
├── evals/                   # A/B 实验：prompt、硬断言、记录表
└── docs/                    # 接口落盘 + stage-layout / arkui-syntax / ui-style
```

**模板对照**（写在 `SKILL.md`，reference 各有一份要点）

画中画 `PiPTemplateType`（`controlGroups` 必须同族）：

| 用户说法 | 模板 |
|----------|------|
| 播放、点播、看视频 | `VIDEO_PLAY` |
| 通话、语音/视频电话 | `VIDEO_CALL` |
| 会议、开会 | `VIDEO_MEETING` |
| 直播 | `VIDEO_LIVE` |

闪控球 `FloatingBallTemplate`：

| 用户说法 | 模板 | 必传 |
|----------|------|------|
| 静态、图标+标题、不刷新文案 | `STATIC` | title + icon，禁止 update |
| 标题+内容 | `NORMAL` | title |
| 强调、图标+标题+内容 | `EMPHATIC` | title |
| 只要一行标题 | `SIMPLE` | title |

闪控窗 `FloatViewTemplateType`（系统只有两种窗框）：

| 用户说法 | 模板 |
|----------|------|
| 圆角小窗、方窗、面板 | `ROUNDED_RECTANGLE` |
| 横条、细条、底部条 | `HORIZONTAL_BAR` |

未说样式：PiP=`VIDEO_PLAY`，球=`EMPHATIC`，窗=`ROUNDED_RECTANGLE`。

**主流程**：命中 Skill → 分流 → 对照模板 → 按模式读最少文件 → 产出。刷新官网仅用户明确要求。

**影响面**：本 Skill 目录与 GitHub 仓库。不改 WMS。

---

## 五、接口设计

不设计。不新增、不修改 HarmonyOS 对外接口。

---

## 六、测试用例

写程序验收：每种系统模板一条。调用顺序与下面 demo 相同，**只改模板枚举和必传字段**。完整骨架见 `examples.md`。

共用流程 demo（默认样式）见下；其余样式把对应行换成表里的枚举即可。

### 画中画（4）

共用：探测 → `create` + `startPiP` → `stateChange`；不在 `aboutToAppear` 里 start。

```ts
this.pip = await PiPWindow.create({
  context: ctx,
  componentController: this.xCtrl,
  templateType: PiPWindow.PiPTemplateType.VIDEO_PLAY, // 按下表替换
  contentWidth: 1920,
  contentHeight: 1080
}, node);
await this.pip.startPiP();
```

| # | 用户说法 | 模板 | 期望 |
|---|----------|------|------|
| P1 | 播放 / 点播 / 看视频 | `VIDEO_PLAY` | 控件组用播放族；101 与 102 不同时加 |
| P2 | 通话 | `VIDEO_CALL` | 控件组用通话族 |
| P3 | 会议 | `VIDEO_MEETING` | 控件组用会议族 |
| P4 | 直播 | `VIDEO_LIVE` | 控件组用直播族 |

### 闪控窗（2）

共用：`FLOAT_VIEW` 弹窗 → `setUIContext` → `onStateChange` → `start`。

```ts
this.fv = await floatView.create({
  context: ctx,
  templateType: floatView.FloatViewTemplateType.ROUNDED_RECTANGLE // 按下表替换
});
await this.fv.setUIContext('pages/FloatPanel');
await this.fv.start();
```

| # | 用户说法 | 模板 | 期望 |
|---|----------|------|------|
| W1 | 圆角 / 方窗 / 面板 | `ROUNDED_RECTANGLE` | 默认窗框 |
| W2 | 横条 / 细条 | `HORIZONTAL_BAR` | create 即用横条，或 `switchTemplate` 切过去 |

### 闪控球（4）

共用：不弹窗 → `create` → `startFloatingBall`；有 `click`。

```ts
await this.fb.startFloatingBall({
  template: floatingBall.FloatingBallTemplate.EMPHATIC, // 按下表替换
  title: '盯盘',
  content: '沪指 +0.8%'
});
```

| # | 用户说法 | 模板 | 期望 |
|---|----------|------|------|
| B1 | 强调 / 图标+标题+内容 | `EMPHATIC` | title 必传，可带 icon、content |
| B2 | 标题+内容 | `NORMAL` | title 必传 |
| B3 | 只要一行标题 | `SIMPLE` | 只传 title |
| B4 | 静态图标、不刷新文案 | `STATIC` | title + icon；禁止 `updateFloatingBall` |

### 高级（2）

| # | 用户说法 | 期望 |
|---|----------|------|
| A1 | 点球展开窗、点窗缩小回球 | 两边都 create 且未 start → `floatView.bind` → 再 start 一侧；听 `IN_FLOATING_BALL`；不要先 start 再 bind，不要自己 start 两套手动切 |
| A2 | 闪控窗持续展示 + 窥视保护敏感内容 | `dlpAntiPeep`；STARTED 后用闪控窗 `windowId` 做 `setAntiPeepMaskLayer`；不要编 `FloatViewController` 防窥 API，不要用主窗 id |

**必过门禁**：单测 □ · 编译 □ · codecheck □ · 兼容结论 □（本次不适用；P1–P4、W1–W2、B1–B4、A1–A2 人工各走一遍）

---

## 评审结论（会后填）

- [ ] 通过
- [ ] 修改后通过（待办：……）
- [ ] 不通过（原因：……）

**待办 / 责任人**：……
