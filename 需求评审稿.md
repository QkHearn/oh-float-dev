# 需求评审稿

> 一页说清：做什么、做成什么样、怎么验。对象是 Cursor Skill **float-kit**，不是某一款应用业务。

---

## 一、需求内容

**一句话**：做一个不依赖其他 Skill 的 Cursor 项目 Skill，辅助 Agent 完成画中画 / 闪控窗 / 闪控球三类悬浮需求的分流、接口问答、查错、写 ArkTS、写评审稿。

**背景 / 痛点**：这三类窗易用错模块、把 `create` 当成已显示、权限配不全、未绑定并行启动。官网页常拉成空壳，Agent 现场抓文档不可靠。需要把契约、误用清单、骨架和评审模板放进同一个 Skill 目录。

**范围**
- 做：`PiPWindow`、`floatView`、`floatingBall`；球窗绑定 `floatView.bind`；闪控窗 + `dlpAntiPeep` 防窥组合；五种工作模式（分流 / 接口 / 查错 / 写程序 / 需求设计）；接口与权限资料落盘在 `docs/`。
- 不做：子窗 `createSubWindow`；全局悬浮窗 `TYPE_FLOAT`；模态窗 `TYPE_DIALOG`；依赖 `req-design` 或其他 Skill；日常 WebFetch 官网当权威；用本仓 DTS/`docs/zh-cn` 覆盖落盘签名。

**成功标准**：提到这三类窗时 Agent 先分流再动手；范围外给一句理由后停手；查错标到文件/行；写程序含探测、权限、回调、退出；评审稿用本 Skill 自带六章模板；不引用其他 Skill。

---

## 二、规格设计

| # | 规则 | 说明 |
|---|------|------|
| 1 | 先分流，再按一种能力产出；一次请求可叠加模式，不可同时生成三套窗代码 | 正常行为 |
| 2 | 日常契约以 `docs/` 接口落盘为准；仅用户说「同步官网 / 刷新 skill」且抓到正文时覆盖；空壳则不动落盘 | 资料权威 |
| 3 | `reference-*.md` 不列接口表；签名以 `js-apis-*.md` 对应章节为准，按标题 Grep/Read，不通读全文 | 调用方限制（Agent 读文件） |
| 4 | 范围外（子窗 / TYPE_FLOAT / 模态窗）明确拒绝，不硬套三类 API | 异常表现 |
| 5 | 写程序/查错必须检查互斥：PiP、未绑定闪控窗、未绑定闪控球不可并行（`1300034`）；绑定后启停任一即两边一起停 | 正常 + 异常 |
| 6 | 生成代码遵守：`create≠start`、状态以回调 `STARTED` 为准、主窗已 show/前台、先 `canIUse` + `isXxxEnabled` | 正常行为 |
| 7 | 查错每条必须含位置、现状、错因、表现、改法；禁止只说「用法不对」 | 可验收输出 |
| 8 | 需求设计只 Read 本目录 `req-template.md`，不依赖其他 Skill | 兼容 / 自包含 |

**关键约束**

- Skill 名：`float-kit`；目录：`.cursor/skills/float-kit/`
- 能力起始：PiP API11（typeNode API12+）；闪控球 API20；闪控窗 API26；仅 Stage
- 权限：PiP 无特殊权限；`FLOAT_VIEW` user_grant（声明 + ACL + 弹窗）；`USE_FLOAT_BALL` / `DLP_GET_HIDE_STATUS` system_grant（声明 + ACL，不弹窗）
- 指导缺口：`guide-*.md` 为摘要，不得当官网全文；PiP/闪控球无指导落盘时以接口 + `examples.md` 为准
- 机型 / syscap：`SystemCapability.Window.SessionManager`；防窥另加 `SystemCapability.Security.DlpAntiPeep`

---

## 三、功能点

1. **F1** 按场景分流到 pip / float_view / floating_ball / bind / float_view+peep / out_of_scope
2. **F2** 接口模式：输出导入、权限、调用顺序、约束、错误码对照表
3. **F3** 查错模式：对照误用清单，按 E 格式标到文件/行
4. **F4** 写程序模式：Stage ArkTS 骨架（PiP 默认 typeNode；已有 XComponent 走旧路径）+ 权限 JSON
5. **F5** 需求设计模式：按 `req-template.md` 出六章评审稿
6. **F6** 资料落盘与刷新：`docs/` 为契约；用户触发才同步官网
7. **F7**（可选 / 二期）补齐画中画、闪控球官网指导全文落盘

---

## 四、方案设计

**思路**：入口 `SKILL.md` 只管分流、铁律、五种模式的读什么/产出什么。领域细节在 `reference-*.md` 与 `examples.md`。签名与权限正文在 `docs/`。评审模板在 `req-template.md`。不引入其他 Skill、不嵌套工作流。

**主流程**
1. description 命中关键词或用户点名 float-kit → 加载 `SKILL.md`
2. 用分流表判定能力；范围外停手
3. 按模式读最少文件并产出（接口表 / E 列表 / ArkTS / 六章评审稿）
4. 仅当用户要求刷新时 WebFetch 官网覆盖 `docs/`

**影响面**：Cursor 项目 Skill 目录；GitHub `QkHearn/oh-float-dev`；不改窗口框架代码。

**风险 & 取舍**
- 风险：官网抓页空壳导致指导过期 → 应对：缺口表明示；空壳不覆盖；刷新失败要说明
- 曾考虑以本仓 DTS 或个人 `req-design` 为权威，未采用：用户要求以官网落盘为准、Skill 自包含

---

## 五、接口设计

HarmonyOS 公开接口无变更，应用侧沿用现有 `PiPWindow` / `floatView` / `floatingBall` / `dlpAntiPeep`。

本 Skill 对外契约（Agent 行为接口）：

| 接口 | 变更 | 可见性 | 备注 |
|------|------|--------|------|
| 分流表 | 新增（Skill 内） | 项目 Skill | 判定 pip / 窗 / 球 / bind / 防窥 / 范围外 |
| 查错 E 格式 | 新增（Skill 内） | 项目 Skill | 位置、现状、错因、表现、改法；红黄绿 |
| `req-template.md` 六章 | 新增（Skill 内） | 项目 Skill | 不依赖其他 Skill |
| `@ohos.PiPWindow` 等 | 沿用 | public | 签名以 `docs/js-apis-*.md` 为准 |

---

## 六、测试用例

| # | 场景 | 期望 | 对应规则/功能点 |
|---|------|------|-----------------|
| 1 | 用户说「视频退出后台还要播，系统控制条」 | 判为画中画；不生成闪控窗/球代码 | F1 / 规则1 |
| 2 | 用户说用 `TYPE_FLOAT` 做闪控 | 说明范围外并停手，不套 floatView | F1 / 规则4 |
| 3 | 用户贴 `aboutToAppear` 里 `startPiP` | 至少一条 E：主窗未 show，含位置和 1300013 改法 | F3 / 规则6、7 |
| 4 | 用户要盯盘面板 + 贴边球切换 | 用 `bind`，不并行 start 两套 | F4 / 规则5 |
| 5 | 用户问闪控窗权限 | 表中写明 FLOAT_VIEW 三处 + 只对它弹窗 | F2 / 规则2 |
| 6 | 设备不支持 / `isXxxEnabled` 为 false | 生成代码走 801 失败路径，不假装成功 | F4 / 规则6 |
| 7 | 已有 PiP 再 start 闪控窗（未 bind） | 查错或写程序指出 `1300034` | 规则5 |
| 8 | 用户说「写评审稿」做闪控窗需求 | 只读 `req-template.md` 出六章；不做子窗/TYPE_FLOAT 写进范围 | F5 / 规则8 |
| 9 | 用户说「同步官网」但页面空壳 | 不覆盖 `docs/`，并说明未刷新成功 | F6 / 规则2 |
| 10 | 连点 start / create 后当已显示 | 指出重复启停或 `create≠start` | 规则6 |

**必过门禁**：单测 □（Skill 无自动化，用上表人工走查） · 编译 □（本次不适用） · codecheck □（本次不适用） · 兼容结论 □（不改系统 API，沿用公开接口）

---

## 评审结论（会后填）

- [ ] 通过
- [ ] 修改后通过（待办：……）
- [ ] 不通过（原因：……）

**待办 / 责任人**：……
